% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc_year.R
\name{desc_year}
\alias{desc_year}
\title{Compute yearly epidescriptors}
\usage{
desc_year(
  data,
  cases,
  time,
  space,
  descriptors,
  pop = NULL,
  sweek = 1,
  collapse53 = TRUE
)
}
\arguments{
\item{data}{Data frame with the input data.}

\item{cases}{Character. Name of the variable in \code{data} that contains the
case counts.}

\item{time}{Character. Name of the variable in \code{data} that contains the
\emph{epiyearweek} date temporal identifier (see \code{\link[=epiyearweek]{epiyearweek()}}). It must be of
type character.}

\item{space}{Character. Name of the variable in \code{data} that  contains the
spatial identifier for which a case count time series is available.}

\item{descriptors}{A named list with the descriptor fun and their arguments.
See details.}

\item{pop}{Character. Name of the variable in \code{data} that contains the
population counts. Only required if the descriptor 'Inc' (incidence) is used.}

\item{sweek}{Integer between 1-52 that determines the first week of
the season for which the descriptors will be calculated; e.g. \code{sweek=40} will
compute descriptors between week 40 and week 39 next year. Defaults to 1.}

\item{collapse53}{If TRUE (default), collapse week 53 such that
half of the cases are assigned to the previous (w52) and next (w01) weeks.
It is recommended so that all years have the same number of weeks.}
}
\value{
A data frame with the resulting descriptors for every
epidemiological year.
}
\description{
Compute yearly epi descriptors based on weekly case counts.
}
\details{
The argument \code{descriptors} consists of a nested list where:
\itemize{
\item The name of the first level will determine the name of the descriptor.
\item The elements of the first levels should also be a list with elements
\code{fun} (descriptor type) and the parameters \code{n}, \code{x} or \code{p} if necessary.
}

Please see the example to see it in practice.
}
\examples{
# Spatiotemporal
data(dengueRio)
dengue <- dengueRio

# Prepare epiyearweeks
dengue$yearweek <- epiyearweek(dengue$date)

# List of descriptors to be computed
descriptors <- list(
  "Ap" = list(fun = "Ap"),
  "Tp" = list(fun = "Tp"),
  "Cnf" = list(fun = "Cnf", n = 3, x = 5),
  "Cnf2" = list(fun = "Cnf", n = 6, x = 5),
  "Cmax" = list(fun = "Cmax", x = 5),
  "Cmed" = list(fun = "Cmed", x = 5),
  "p" = list(fun = "p", x = 5),
  "Cwf" = list(fun = "Cwf", n = 3),
  "Cwf2" = list(fun = "Cwf", n = 6),
  "Cwmax" = list(fun = "Cwmax"),
  "Cwmed" = list(fun = "Cwmed"),
  "Inc" = list(fun = "Inc", p = 1e5)
)

# Compute them
res <- desc_year(
  dengue,
  cases = "cases",
  time = "yearweek",
  space = "muni_code",
  pop = "pop",
  sweek = 40,
  descriptors = descriptors
)
}
\seealso{
\code{\link[=desc_list]{desc_list()}} to access the list of descriptors and its parameters.
}
